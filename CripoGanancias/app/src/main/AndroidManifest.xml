<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.com.lugman.appfinal2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Mycuenta" />
        <activity
            android:name=".MonedaDescripcion"
            android:label="@string/title_activity_moneda_descripcion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Registrar" />
        <activity android:name=".Mismonedas" />
        <activity android:name=".Historial"></activity>
    </application>

</manifest>